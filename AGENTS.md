# ggchord 开发协作规则

本文件是仓库级、跨版本的长期开发约束。处理本仓库中的任何开发、修复、文档或
发布任务时都必须遵守；某一版本独有的需求不得写入本文件，应写入该版本的设计文档。

## 开始工作前的读取顺序

1. 先读本文件。
2. 再读 [`design-roadmap/DESIGN-ROADMAP.md`](design-roadmap/DESIGN-ROADMAP.md)，
   确认当前正式版、正在开发的版本、后续版本边界和跨版本待办。
3. 任何代码或版本文档变更都必须再读
   [`design-roadmap/versions/`](design-roadmap/versions/) 中对应版本的完整文档；
   不得只根据总路线图中的摘要开发。
4. 只有在调查历史接口、迁移背景、回归来源或发布验收时，才读
   [`design-roadmap/DESIGN-HISTORY.md`](design-roadmap/DESIGN-HISTORY.md) 及相关
   历史版本文档。历史文档只描述当时事实，不代表当前接口或当前开发要求。
5. 用户迁移与已发布行为以 `NEWS.md`、对应版本最终验收记录和实际代码为事实来源；
   若三者不一致，先指出差异，不得自行选择一种说法覆盖其他记录。

## 路线图维护

- 用户在会话中补充、删除或调整任何开发要求时，应在同一轮工作中及时更新
  `design-roadmap/versions/` 下对应版本文档，不得遗漏关键约束、非目标、验收条件
  和已作出的取舍。
- 只有当版本状态、主题、范围摘要、顺序或跨版本事项改变时，才同步更新总路线图；
  不把详细实现清单重新堆回总路线图。
- 当前版本正式发布后，从总路线图的“正在开发”位置移除其未完成计划表述，按实际
  发布结果整理到该版本文档，并将它加入 `DESIGN-HISTORY.md` 的已发布索引；然后在
  总路线图中明确新的当前开发版本。
- 补充要求若与路线图、对应版本文档、现有公开契约或另一条用户要求冲突，必须指出
  具体冲突、影响和可选处理方式，请用户选择后再改动冲突部分；不得静默覆盖、折中
  或同时保留无法兼容的要求。
- 新发现但未获确认的想法应标为“待确认”，不能写成已决定的版本承诺；已实现、
  已验收、已发布三种状态必须严格区分。

## 版本分支与发布

- 每次开始开发新版本前，必须从主分支创建该版本专用 Git 分支；默认命名为
  `codex/vX.Y.Z`。不得直接在主分支开发新版本。
- 一个版本的全部实现、测试、文档和验收记录都在其版本分支完成。只有该版本已经
  正式发布到 GitHub 后，才可将版本分支整合回主分支；本地验收完成、创建 tag 草稿
  或准备发布均不等同于正式发布。
- 默认直接使用保存在 OneDrive 中的本地 checkout，不创建独立 worktree，不复制或
  额外同步仓库目录；除非用户明确要求，不自动 push、创建 GitHub Release 或发布。
- 稳定阶段及时提交，并保持提交范围单一；不得覆盖或清理用户已有的无关改动。

## 长期设计原则

### ggplot2 职责与图层独立性

- `aes()` 负责数据映射，`scale_*()` 负责映射规则和 guide，`geom_*()` 负责几何与
  固定样式，坐标系负责全局坐标，`theme_*()` 负责非数据外观。
- 固定颜色、透明度等可以直接写入 geom；数据驱动差异必须使用映射与标度。
- 每个图层拥有独立的 `layer_id`、`data`、`mapping` 和参数，同类图层不得互相覆盖；
  用户提供的 scale 始终优先于自动推断的默认 scale。
- 默认绘图应保持稳定。接口迁移可以改变参数归属，但默认配色、布局、标签和图例
  只有在修复明确问题时才改变，并记录到 `NEWS.md`。

### 命名、兼容与参数格式

- 表格字段参考 BLAST 的紧凑命名：单序列使用 `accver`，连接使用
  `qaccver`/`saccver` 及 query/subject 坐标前缀；允许任意自定义标识，不自动添加
  或删除 accession version。
- 破坏性变更必须给出可执行的迁移方向并记录删除版本；同一语义的新旧入口不得
  同时使用。v1.0.0 发布后再开始长期兼容承诺。
- `mapping`、`data`、`position`、`show.legend` 和 `inherit.aes` 遵循 ggplot2。
- 逐序列几何参数可接受单值、等长无名向量、按 `accver` 命名的向量及现有等价
  列表；gene/feature 的 offset、width 和固定标签位置还可按 `+`/`-` 链指定。
- 上述列表语法不扩展到 aesthetic、scale、guide 或 theme。按 `accver` 命名的向量
  是长期推荐写法；低频格式留到 v0.99.0 审计。

### 第三方设计与代码命名

- 可按需要使用第三方字体、图标、图片、专有配色及其他资产；复用具体代码或资产时
  记录许可与来源。
- 代码中的函数名和参数名必须保持通用，不写入第三方品牌、产品、软件或机构名称；
  文档、示例和视觉资产可以按实际需要明确展示或说明参考对象。

先前设计文档已经确认以下借鉴关系。开发相关功能时应读取对应版本文档中的具体
边界；“借鉴”不表示复制其 API，也不得把第三方名称写入 ggchord 的函数名或参数名。

| 参考对象 | 借鉴或比较的方面 |
| --- | --- |
| ggplot2 | 图形 grammar，以及 `aes()`、scale、guide、geom、coord、theme 的职责划分；公开图层签名、staged mapping 和用户 scale 优先规则应与其生态一致。 |
| BLAST | 生物信息表格的紧凑字段命名：`accver`、`qaccver`/`saccver` 和 query/subject 坐标前缀；这是命名参考，不代表在绘图阶段运行 BLAST。 |
| Circos | 环形信息层级、克制的 ribbon 表达和多层环形图的整体阅读秩序。 |
| clinker | 适合发表的基因箭头表现、基因簇与共线性关系的可读表达。 |
| DNA Features Viewer | 基因和通用注释的可读性、注释冲突处理及局部特征布局。 |
| SnapGene、Geneious | 单序列圆图的信息层级，局部特征标签、拥挤隐藏以及密集注释下的可读性。 |
| gggenomes | tidy 多表输入、`focus`/`sync`、feature stacking、局部锚定和局部翻转等共线性工作流。 |
| genoPlotR、gggenes | 直线型基因簇和多序列共线性表达，用于比较 `coord_collinear()` 的语义与表现。 |
| circlize | 多轨道扩展能力，用于比较未来显式 track/ring 契约；不照搬其接口。 |
| ggview | 按真实导出尺寸进行静态预览的流程；正式保存仍使用标准 `ggsave()`。 |

## 测试、文档与发布验收

- 一般代码、文档和局部几何修改不运行包级发布检测流程，包括
  `testthat`、`devtools::test()`、`R CMD build`、`R CMD check`、vignette 重建和
  PNG/PDF/SVG 发布设备矩阵。普通开发只做与当前改动直接相关的轻量
  验证，不将局部修改自动升级为包发布验收。
- 只有当用户明确说“要在 GitHub 上正式发布当前版本”或表达同等的明确
  发布授权时，才运行上述包级检测和完整发布验收。“准备”、“继续开发”、
  “优化”、“修复”或“本地验收”不等同于该授权。
- 默认 testthat 只保留公开 API 的最小行为：`ggchord()` 的一次基础 build、核心 geom
  组合、三种标签布局的代表输入、validate/clean/三个 import/三个 ribbon utility 的
  单一成功路径、region/feature/link 的单一成功路径、一个基本错误输入，以及每个已
  确认严重缺陷的单一回归。
- 不得用精确浮点坐标、文字框尺寸、迭代次数、重复设备矩阵、固定秒数、guide ggproto
  字段、同一参数等价格式穷举、重复构建/序列化/文件输出、内部 scale 标记或其他无
  公开行为意义的内部实现细节锁定实现。
- 已确认的几何缺陷可通过内部检测器验证公开布局的碰撞、方向和数据保留结果；完整
  设备矩阵、性能基准和复杂布局质量检查放在 `tools/` 的显式验收脚本中。
- 日常小阶段可运行一个最小可复现脚本、单一代表性 build 或直接几何断言；
  修改 roxygen 时可生成 Rd。跨模块重构、依赖变化或疑似全局回归也不得自动
  触发包级检测；先记录风险和待发布验收项。
- 在用户明确授权 GitHub 正式发布后，版本候选必须通过完整 testthat；
  正式发布前必须通过 `R CMD check`、英语/汉语
  vignette 和 PNG/PDF/SVG 验收。新的公开参数至少有一个可运行文档示例。
- 破坏性变化必须写入 `NEWS.md` 和迁移表；默认示例外观不得无理由变化；多个同类
  图层、正反 orientation、不同 radius/curvature/gap 是核心人工验收场景。
- v1.0.0 前避免无必要地反复重写整套用户文档；但与当前变更直接相关的 NEWS、
  roxygen、Rd、版本设计文档和迁移说明必须同步更新。

### GitHub Release 说明

- 每个 GitHub Release 必须同时提供现代汉语和英语说明，顺序固定为现代汉语在前、
  English 在后；标题分别使用“现代汉语”和 `English`。
- 面向用户标注语种时统一使用“汉语”或“现代汉语”，不得使用“中文”；README、
  vignette、网站、迁移指南和其他发布材料同样遵守。
- 两种语言必须覆盖同一组版本事实，包括版本概览、主要变更、破坏性变化与迁移、
  验证结果和安装方式，不得将其中一种缩减为缺少关键内容的摘要。
- 发布说明以对应版本的 `NEWS.md`、最终验收记录和实际发布资产为事实来源，不写入
  未实现计划；弃用或删除必须给出替代写法和预计删除版本。
- 正式发布或修改说明后，逐项核对 tag、版本号、比较链接、附件、校验和、草稿/
  预发布状态，以及两种语言内容是否对应。

## 重要目录与文件

| 路径 | 用途与维护规则 |
| --- | --- |
| `R/` | R 包实现源码；公开接口的 roxygen 注释也以这里为源。 |
| `tests/testthat/`、`tests/testthat.R` | 轻量公开行为测试与严重缺陷回归测试。 |
| `tools/` | 性能、几何、标签、输出和发布验收脚本；保留在仓库中，但不进入发布包。 |
| `data-raw/` | 随包数据的可重复生成规则；明确记录抽样、过滤和展示链重编码。 |
| `data/` | 小型、清晰、可由 `data-raw/` 重建的随包教学数据。 |
| `examples/` | 原始 FASTA、GFF3、BLAST、TSV 等示例输入和示例脚本；不得为改善视觉效果篡改原始记录。 |
| `vignettes/` | 英语和汉语长篇教程的源文件。 |
| `inst/` | 安装时随包提供的辅助文件；当前包含拼写检查词表 `WORDLIST`。 |
| `man/`、`NAMESPACE` | roxygen 生成并跟踪的公开 Rd 和命名空间；被文档引用的 `man/figures/` 也跟踪。 |
| `README.md`、`README-Hans.md` | 英语和汉语入口说明；保持简洁，详细功能留给 vignette/reference。 |
| `DESCRIPTION` | 包元数据、当前开发版本号、依赖和项目链接。 |
| `NEWS.md` | 面向用户的已实现版本变化和迁移事实，不记录尚未实现的愿望清单。 |
| `design-roadmap/` | 权威设计路线图目录；包含总路线图、历史索引和各版本详细文档。 |
| `docs/` | 暂时冻结的 GitHub Pages 生成物；网站迁移到独立发布分支或 Actions 前，日常功能提交不得重建。 |
| `doc/`、`Meta/`、`pkgdown/` | 构建或检查产生的中间产物，不进入 Git。 |
| `.github/workflows/` | GitHub Actions CI 与发布自动化配置。 |
| `_pkgdown.yml` | pkgdown 网站源配置；网站冻结期间仅在明确的网站任务中修改。 |
| `.Rbuildignore`、`.gitignore` | 分别控制 R 包构建内容和 Git 跟踪范围，新增开发专用目录时同步检查。 |
| `LICENSE`、`LICENSE.md` | 包许可证及其说明，不得在普通功能开发中改动。 |
| `ggchord_*.tar.gz` | 本地生成的源码包构建产物，不是实现或文档的事实来源，不得手工编辑。 |

## 协作约定

- 默认使用中文沟通。
- 只修改当前任务与对应版本直接相关的文件；不自动重建冻结网站，也不进行与任务无关
  的大规模清理或极端压力测试。
- 原始输入、生成数据、文档示例和代码行为之间必须可追踪；缺失业务数据时不得伪造值。
