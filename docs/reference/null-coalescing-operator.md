# NULL coalescing operator

Returns `y` if `x` is NULL, otherwise returns `x`.

## Usage

``` r
x %||% y
```

## Arguments

- x:

  Any R object (may be NULL)

- y:

  Default value returned when `x` is NULL
