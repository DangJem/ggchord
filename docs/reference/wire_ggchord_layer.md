# Attach the shared plot reference and a lazy data function to a ggchord layer

Attach the shared plot reference and a lazy data function to a ggchord
layer

## Usage

``` r
wire_ggchord_layer(lyr, plot)
```
