# Validate the gene leader-line linetype argument

\`gene_label_segment_linetype\` accepts the special value \`"auto"\`
(solid lines, except dashed for labels moved to the other side of their
arc) or any valid ggplot2 linetype (character name or numeric dash
pattern).

## Usage

``` r
validate_gene_segment_linetype(lt)
```
