# Rebuild straight leader-line segments after a final label de-overlap pass.

Rebuild straight leader-line segments after a final label de-overlap
pass.

## Usage

``` r
ggchord_repel_segments(gl, min_segment_length = 0.5)
```
