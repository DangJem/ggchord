devtools::document()
pkgload::dev_help('ggchord')
devtools::check() # 等价于R CMD check --as-cran，模拟CRAN审核
devtools::check() # 等价于R CMD check --as-cran，模拟CRAN审核
devtools::check() # 等价于R CMD check --as-cran，模拟CRAN审核
devtools::check() # 等价于R CMD check --as-cran，模拟CRAN审核
devtools::check() # 等价于R CMD check --as-cran，模拟CRAN审核
devtools::check() # 等价于R CMD check --as-cran，模拟CRAN审核
tools::Rd2pdf("man/your_function.Rd")
devtools::build_vignettes()
pkgdown::build_site() # 生成网站
pkgdown::build_site() # 生成网站
setwd("D:/Data/OneDrive/R语言/ggchord")
pkgdown::build_site() # 生成网站
devtools::document() # 自动在man/目录生成帮助文件，并更新NAMESPACE
devtools::test() # 用testthat确保函数功能正确，避免后续修改引入 bug
devtools::build_vignettes() # 生成HTML文件，存于inst/doc/
pkgdown::build_site() # 生成网站
devtools::check() # 等价于R CMD check --as-cran，模拟CRAN审核
devtools::build_vignettes()
devtools::build() # 构建包（生成tar.gz文件）
devtools::build() # 构建包（生成tar.gz文件）
devtools::build() # 构建包（生成tar.gz文件）
devtools::build() # 构建包（生成tar.gz文件）
devtools::build() # 构建包（生成tar.gz文件）
devtools::build() # 构建包（生成tar.gz文件）
devtools::build() # 构建包（生成tar.gz文件）
devtools::check() # 等价于R CMD check --as-cran，模拟CRAN审核
devtools::build() # 构建包（生成tar.gz文件）
devtools::build() # 构建包（生成tar.gz文件）
