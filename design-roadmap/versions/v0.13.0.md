# v0.13.0 — 单序列圆图与生物学注释

**状态：v0.13.0 开发与本地验收阶段，尚未发布。** 专用
`coord_circular()`、通用 feature geometry、Position grammar、内置通用注释数据库
与限制性酶切数据/显示分层均已实现，正在进行开发期轻量验证；发布级回归验收待正式
发布授权后执行。

本版本的质粒视觉契约以
[`../GGCHORD_VISUAL_REFERENCE.md`](../GGCHORD_VISUAL_REFERENCE.md) 为基础；
2026-09-13 用户对 pBluescript II SK(+) 参考图的逐项反馈具有更高
优先级。特别是：restriction-site 保留自己的圆周/扇形排版，不再与
feature 外部标签合并成左右两条直角坐标栏。

## A. 单序列圆图

单条完整基因组不应只是“四序列 chord 少三条序列”的退化情况。v0.13.0 计划
提供明确的单序列布局入口，共享现有 gene/feature/region、scale、theme、guide
和标签组件，但不生成无意义的跨序列 ribbon。

- 支持闭合圆形和带缺口的线性化圆图；
- 基因、调控元件、重复区和用户 region 可放在显式内外轨道；
- 标签可选择贴弧、局部径向或外部 callout，并复用碰撞、换行和省略策略；
- 原点、方向、旋转和坐标标签由 coord/position scale 管理；
- 明确参考 SnapGene、Geneious 和 DNA Features Viewer 的信息层级与可读性；
  可直接使用第三方字体、图标、图片、专有配色及其他资产，但代码中的函数名和
  参数名保持通用，不写入第三方名称。

最小原型比较后采用专用 `coord_circular()`，由它管理缺口、原点旋转和方向；
单序列行为不放入 `coord_chord()` 的条件分支。

## B. Position grammar 与通用 feature geometry

- v0.13 起 `geom_gene()` 的默认 placement 是 `position = "identity"`；v0.12
  隐式 `gene_offset = 0.1` 的迁移写法是 `geom_gene(position = "strand")`；
- signed local-normal 统一规定正值向 sequence 外侧、负值向内侧；标量
  `position_strand(0.1)` 展开为 `+ = +0.1, - = -0.1`，显式命名的链值不再翻转；
- `position_plasmid(-0.1)` 让两条 strand 共用内侧 band，strand 仅控制箭头方向；
- `position_feature_stack(base_position = ...)` 先放置基础 band，再将 track
  作为圆内注释的显式布局层。长且高优先级的 feature 靠近 backbone，
  局部重叠对象向内分层；不重叠的相邻 feature 只在父级覆盖区间内
  可共用层，不得跨越父 feature 末端继续强制同层；
- 默认 stacking 顺序先服从显式 `display_priority`（大值优先）或
  `prioritized_display = TRUE`，然后严格按 span 由长到短分配；每个 feature
  始终选择最近的无冲突层，不再为局部相邻关系保留较深层。
  默认内侧 plasmid band 中，track 0 最靠近 DNA backbone，更深 track
  逐步靠近圆心；
- feature type、名称和颜色不得单独决定 track，不使用 AmpR、lacZα、ori
  等名称特例。但内部布局明确区分 backbone、feature band、feature-label
  gutter 和更深的 label-only track；`find_common_features()` 不输出语义
  `preferred_lane`；
- `geom_gene()` 保留 gene role、gene scale、strand guide 和专用入口，但 polygon
  由 `geom_gene()`/`geom_feature()` 共用的 interval、local frame、shape factory
  与 short-feature pipeline 产生；
- `geom_feature_plasmid()` 是该通用引擎的单质粒快捷方式；multi-segment
  feature 在 layout 与 stacking 阶段始终是一个 biological feature，共享
  track、方向和唯一 label；只在几何阶段按 segment color、gap 和局部
  分界细分，整个 feature 只在真实方向端生成箭头。segment membership 本身不画
  虚线分界；只有源注释中的 cleavage arrow 或显式非实线 `line_style` 才生成分界；
- 通用 `feature_shape` 扩展为 `arrow`、`compact_arrow`、
  `promoter_arrow`、`primer_arrow`、`marker`、`block`、`chevron` 和
  `lollipop`；这些是用户可显式选择的 geometry，不由 feature type 强制。
  nondirectional 默认无箭头，forward/reverse/bidirectional 则服从 feature 自身
  directionality 生成对称的单端/双端方向表达；
- zero-length/point feature 不会被放大为伪区间 polygon，而是在 DNA
  backbone 上绘制短径向 tick。极短 directional feature 围绕真实 midpoint
  建立最小显示跨度，与长箭头共用 body、shoulder 和 head 拓扑；真实
  start/end 和 leader anchor 不改变。promoter、primer 及 compact arrow 保留
  同一拓扑，但采用更窄的类型宽度比例；
- 存在 feature label layer 时，feature thickness 依最终字号在有限上下界内
  调整；超长 label 不会使 feature 无界加厚，而是进入 label fallback；
- `gene_offset`/`feature_offset` 仅作为带 warning 的迁移入口，不能与非 identity
  Position 同时使用；`gene_width` 等纯 geometry 参数继续保留。

Feature label 是 feature layout 的第二阶段：track assignment → geometry →
feature-owned anchor → label collision resolution → fallback。默认依最终字体与
设备 bbox 依次选择 `inside`、`adjacent`、`external` 或 hidden；标签只可
改变 anchor、offset、内外状态和可见性，不改变 feature 的 genomic start/end/
midpoint/direction。Internal label 切向正置，其走向由文字所在半径的局部切线
决定，并按 fill 亮度选黑/白字。算法先根据 feature band 之间的真实空间建立
一个或多个固定半径的文字轨道；标签先选择半径，再按每个字符的实际前进宽度
沿该半径圆逐字排布，并分别计算字符所在位置的局部切线，形成真正弯曲的文字
基线，而不是将整段文字当成一条直线旋转或在平面中平移。布局冲突
与 leader 裁切使用同一组逐字符包络，不再使用整段直线 bbox。lane 0 与
lane 1 之间保留至少两条实测文字轨道的主走廊，lane 1 与 lane 2
只保留紧凑几何间隙，lane 2 内侧建立共享 label-only tracks。lane 0 文字
优先进入主走廊，lane 1/2 文字优先进入特征簇内侧轨道；
只有这些内部候选全部失败才转为 external callout；明显移位的内层标签使用短
leader。External label 不占用 restriction-site 的圆周标注带。`external = FALSE`
时，内部与紧邻位置均失败的 label 会隐藏，
不再无限向圆心推移。`coord_circular(fit = "labels")` 依最终 bbox 扩展
外围边界，保证 external label 不被裁切。

视觉回归范围覆盖 pBR322、pUC19、pBluescript II SK(+)、pSB1C3、pET-28a(+)、
pETDuet-1、pcDNA3.1(+)、pTRE-Tight-BI、pSpCas9(BB)-2A-GFP (PX458)、pDONR221、
pCAMBIA1300、pEarleyGate 201 和 pTRIPZ，并增加完全重叠、partial overlap、containment、chained overlap、等长
重叠、显式优先级短 feature、point feature、multi-segment feature、超长 label
和极短 directional feature 的 synthetic stress fixture。上述质粒只是回归参考，
实现中严禁使用质粒名、feature 名或颜色特例。

## C. 限制性酶切位点

计划拆成数据计算和几何表达两层：

```r
sites <- find_restriction_sites(sequence) |>
  filter_restriction_sites(set = "unique_6plus")

ggchord(seq_data, gene_data = gene_data) +
  geom_seq() +
  geom_restriction_site(data = sites)
```

- `find_restriction_sites()` 只进行搜索和切点计算，返回稳定 pattern identity、
  酶名、识别序列、1/2/4/unknown cleavage、切割位置、末端类型及来源；
- 酶数据库版本必须可追踪，用户也可传入自定义 motif；
- `filter_restriction_sites()` 独立处理 unique、长度、供应商、窗口等显示筛选；
- `geom_restriction_site()` 在单个 geom 中负责刻线、标签和指示线；
- 真实位点只决定 anchor，文字位置由实际 bbox 驱动的混合排版器决定；
  稀疏标签保持自然径向轮廓，侧面密集簇自动切换为共享内边界、固定行距的
  stacked label column，并严格保留 genomic order；
- leader 在条件合适时直接连接，密集或分流标签使用独立的径向 stub
  和 fan segment 两段折线；密集簇先以短根段形成窄线束，再有序展开，
  不共享主干；
- leader 始终连到酶名所在的左/右文字边缘：向右走时连左缘，向左走时连右缘；
  接近垂直时使用该侧最近的左下/右下（或左上/右上）角，不改接上/下边中心；
  酶名自动放在接线一侧；
- 可按酶、切割次数和窗口过滤，重复名称仍保留全部位点；
- 默认复合标签用两个同一 bbox 管理的 grob 渲染：仅 unique cutter 的酶名使用粗体，
  重复切割的酶名与坐标使用常规字重；
  只合并真正同一切点，相邻 bp 始终保持独立；
- 序列搜索优先使用轻量实现；大型序列可选用 Biostrings，但不设为强制依赖。

计算层必须使用零宽匹配枚举 IUPAC motif 中可重叠的固定 anchor，
不得因 `gregexpr(..., fixed = TRUE)` 的消耗式匹配漏掉候选位点。
保留 REBASE motif 的源大小写，并在计算前将小写标记的反向切割定义
统一到标准的 top/bottom cut 方向。
`misc/embossre.equ` 作为可审计的同等酶映射纳入内置数据库；
`filter_restriction_sites(parent_set = "commercial_nonredundant")` 在 commercial
集合内按识别组和位点保留稳定代表，但不修改原始匹配或切点。

当前 REBASE 609 parser 的规范化主表读取 `VERSION`、`embossa_e.txt`、
`embossa_r.txt`、`embossa_s.txt` 和 `misc/embossre.equ`。发布包内置由这些文件可重复生成的
规范化模式表，并保留版本与来源元数据。`examples/rebase/misc/` 已解压，
其他文件保存 REBASE 的补充信息；构建或更新数据库时可按需用于补充或核验
元数据，但当前已生成数据库的运行时搜索不依赖该目录。

## D. Backbone、标签、主题、scale 与示例体系

- `geom_seq()` 增加 single/double/band backbone；
- `geom_feature_label()`、`geom_feature_label_repel()` 复用 gene label 的文本测量、
  碰撞、leader 与 fitting；默认 feature 模式依据最终字体 family、face、字号、
  lineheight 和设备实测 bbox，依次选择 `inside`、`adjacent`、`external`
  或 hidden，并在
  layout export 中公开 `feature_label_mode`；
- feature 标签使用统一的切向正置规则；放不下时先沿局部切线按基因组顺序错层，
  再搜索有限的相邻径向 lane，然后转为 external。求解器同时避开已放标签、
  实际 feature polygon、圆心保留区和 backbone 边界，只对明显移位的
  adjacent/external 文字绘制裁切到真实 bbox 的
  细灰短 connector；
- feature 内部文字颜色依据最终 fill 的相对亮度自动选择黑色或白色；显式文字颜色
  和 `.dna` 来源的既有颜色优先。小 glyph 在未显式设置 linewidth 时使用较轻描边；
- `geom_seq_center_label()` 显示通用 circular sequence 名称与长度；
- `theme_ggchord_plasmid()` 只定义非数据外观；feature fill/shape 由独立 preset
  scale 提供，任何手动 scale 在任意添加顺序下优先；

- `examples/` 保留原始 FASTA、GFF3、BLAST 和 TSV，不为视觉效果修改源记录；
- `data/` 只保存小型、清晰、可重复生成的教学 fixture；
- `data-raw/` 保存生成规则，明确任何抽样、过滤或展示链重编码；
- pUC19、pBR322、pBluescript II SK(+) FASTA 保持不变；新增小型、可重复生成的
  sequence、核心 feature 与常用 restriction-site fixture；
- 上述十三个参考质粒的 `.dna` 文件作为已知质粒的权威 feature 坐标来源，
  生成脚本记录文件 SHA-256 并将注释内置为精确序列匹配层；
  其他序列仍使用通用检测算法；
- `find_common_features(mode = "auto")` 遵循数据库 `detectionMode`：
  `exactProteinMatch` 先做精确蛋白质匹配，失败后才做近精确 DNA 匹配；
  其余记录做精确 DNA 匹配。显式 `mode = "protein"` 仍保留近似蛋白质搜索；
- 开发阶段不生成文档图片，统一在 v1.0.0 文档重构时出图。

本轮不新增 CDS/promoter/ori 专用 geom，不引入
强制 Biostrings 依赖，也不创建通用 `track_*()`。`coord_collinear()` 留至 v0.14；
coverage/GC 等 quantitative ring 与统一 track contract 留至 v0.16。
feature 与 restriction-site 标签各自保留 bbox、视觉样式和 leader；
restriction-site 的圆周轮廓/局部扇形不得被 feature callout 二次改写。Primer 仍是独立的
第三类注释对象，其公开图层契约留待后续实现，不以 feature 或 restriction-site
替代。

## 2026-09-12 视觉参考实装记录

- 已将十三个 `.dna` 参考记录作为精确匹配层，当前可重复生成 201 个 biological
  feature 与 218 个 segment；另将 pSB1C3 和 pETDuet-1 的 7 个真实 primer
  binding-site 记录保存在独立 `reference_primers` 表中，不并入 Feature；生成元数据
  保存每个源文件 SHA-256 和各类记录数；
- 默认画布为 `#FFFCF5`，双线 backbone 为 `#252525`；feature 采用视觉参考中的
  语义色组，外部 feature 标签使用由最终 fill 派生的浅色圆角 callout；
- 圆周原点 tick 延长且不重复显示 `0` 文本；跨原点 feature 使用 circular midpoint，
  不在零点断成两个 biological feature；
- restriction-site 标签按同一切点组合并保持镜像文字顺序；unique cutter 酶名加粗，
  重复 cutter 使用常规字重；
- 本日曾将 feature 与 restriction-site 的 external 标签进入同一个左右外围
  求解器；该做法已被 2026-09-13 的用户对照结果否定，不再是当前实现契约；
- 本批修改仅完成源数据重建、解析、单一代表性 build/render 和联合 bbox 检查；
  尚未运行新的整包 testthat、vignette 或发布设备矩阵，这些仍属于正式发布验收。

## 2026-09-13 pBluescript II SK(+) 对照修订

- 撤销 2026-09-12 的“feature/restriction 共享左右外围栏”实现；限制性位点
  恢复贴近圆周的自然角度和局部有序扇形，不将稀疏位点拉到远端列表；
- promoter/primer/compact 等方向 feature 统一调用同一环形箭头工厂，
  保留一致的 body/shoulder/head 拓扑，并恢复较窄的类型宽度比例。
  极短箭头围绕真实 midpoint 建立最小显示跨度，不改变数据区间和 leader anchor；
  cleavage/segment 的点线与虚线以显式分段几何输出，避免设备将短虚线画成实线；
- lane 0→1 预留可容纳至少两条实测文字轨道的主通道，lane 1→2
  仅保留轮廓间隙，并在 lane 2 内侧建立共享文字轨道。标签先确定半径，
  再逐字按该圆的局部切线形成弧形基线；冲突检查与引线裁切同样使用
  逐字符包络。内部 track 候选的
  优先级高于外部 callout；pBluescript 的 feature 标签在代表性尺寸下全部保留
  在圆内，密集 MCS 区使用多级标签，内层文字指向较远外层 feature 时绘制短引线；
- pBluescript 专项层级验收为：lane 0 包含 `lacZα`、`lac operator`、
  `lac promoter`、`ori`、`AmpR`、`AmpR promoter`；lane 1 包含
  `f1 ori`、`M13 fwd`、`T7 promoter`、`MCS`、`T3 promoter`、`M13 rev`；
  lane 2 仅包含 `KS primer`、`SK primer`。这是通用 longest-first/最近可用层
  规则的代表性验收结果，不是名称特例；
- 圆形坐标默认目标主刻度数从 3 调整为 5；质粒主/次刻度加长，刻度与
  坐标文字统一放在 backbone 内侧；
- 本轮只做 pBluescript 代表性 build/render、布局导出、SVG 短虚线和 R 语法
  检查；没有运行整包 testthat、vignette 或发布设备矩阵。

## 2026-09-12 Feature Engine 验收记录

- 已实现显式 display priority、longest-first 默认顺序、最近 track reuse、
  type-independent stacking、point tick、multi-segment 单 feature 数据流、受限短箭头、
  字号感知 thickness 以及 inside → adjacent → external → hidden label fallback；
- `tests/testthat/test-feature-engine.R` 保留一组公开行为与严重缺陷回归；在本次视觉
  参考实装之前的 feature-engine 基线曾完成 447 项 testthat、无失败和 warning；
- `tools/validate-feature-engine.R` 现在覆盖十三个已检入的 `.fna`/`.dna`/PNG 参考
  质粒、四类不冒充真实质粒坐标的可重建载体压力场景和一个 synthetic stress 场景，
  并分别检查 feature external-label bbox、restriction 有序扇形、priority、
  point 和 multi-segment 共轨；
- 本次视觉参考实装之前，含英语/汉语 vignette 重建的
  `R CMD check --no-manual` 基线结果为 `Status: OK`；当前改动尚未重新执行发布检查。

## 本版本开发契约

- 直接在保存于 OneDrive 的本地 checkout 开发，稳定阶段提交；不创建独立 worktree、
  不复制或额外同步目录，不自动发布或推送。
- `seq_id` → `accver`，旧输入兼容至 v0.12，v0.13 移除；原始数据文件不改，随包数据重建。
- ribbon 六个端点列必需，`length/pident` 按映射、筛选及统计需求检查，不伪造值。
- gene/feature 注释与分类可选；标识、坐标及 strand 必需。grid 的 unit/arrow 重导出。
- 删除 geom_ribbon_highlight，改用独立 geom_link_ribbon(data = ...) 图层突出子集。
- 修复 feature 形状标度的图例，调高 gene 图例；保留 radial/auto 和设备恢复回归。
- link_branch 可选 none/query/subject，同层同端点同样式共享主干；默认关闭，不聚合原始数据。
- 本轮仅修改包及直接相关文档，不修改网站；不用极端压力测试。
