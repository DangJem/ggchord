% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ribbon-layout.R
\name{bundle_ggchord_ribbons}
\alias{bundle_ggchord_ribbons}
\title{Bundle dense alignment ribbons explicitly}
\usage{
bundle_ggchord_ribbons(
  ribbon_data,
  seq_data,
  bins = 80L,
  min_bundle = 2L,
  weight = c("length", "count", "pident"),
  group_by = NULL
)
}
\arguments{
\item{ribbon_data}{Alignment data in ggchord ribbon format.}

\item{seq_data}{Sequence data containing \code{seq_id} and \code{length}.}

\item{bins}{Positive integer number of normalized midpoint bins per
sequence, default 80.}

\item{min_bundle}{Minimum number of rows required for aggregation, default
2. Smaller groups remain as individual ribbons.}

\item{weight}{Bundle weight: \code{"length"}, \code{"count"}, or
\code{"pident"}. Identity weight is aligned length multiplied by identity
proportion.}

\item{group_by}{Optional character columns that must agree before ribbons
can be bundled.}
}
\value{
A list with \code{data} and \code{report}. The data include
  \code{.bundle_n}, \code{.bundle_weight}, and \code{.bundle_density}; the
  report maps each output row back to its source rows.
}
\description{
Aggregates nearby alignment ribbons on a normalized query/subject grid.
Bundling is always explicit: \code{geom_ribbon()} continues to draw one
ribbon per input row unless the returned data are supplied by the user.
}
\examples{
data(seq_data_example)
data(ribbon_data_example)
bundled <- bundle_ggchord_ribbons(ribbon_data_example, seq_data_example)
nrow(bundled$data)
bundled_plot <- ggchord(seq_data_example, bundled$data) +
  geom_seq() +
  geom_ribbon(ggplot2::aes(ribbon_alpha = .bundle_density)) +
  scale_ribbon_alpha_continuous()
}
