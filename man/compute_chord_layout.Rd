% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{compute_chord_layout}
\alias{compute_chord_layout}
\title{Compute the chord layout}
\usage{
compute_chord_layout(
  seqs,
  lens,
  seq_labels,
  seq_colors,
  seqRadius,
  seq_curvature,
  orientation,
  seq_gap,
  seq_group = NULL,
  seq_group_gap = 0.08,
  seq_group_labels = TRUE,
  seq_group_label_radius = 1.35,
  seq_group_colors = NULL,
  ribbon_data = NULL,
  ribbonGap,
  ribbon_color_scheme,
  ribbon_colors,
  ribbon_alpha,
  ribbon_color_by = NULL,
  ribbon_color_limits = NULL,
  ribbon_color_breaks = NULL,
  ribbon_color_name = NULL,
  ribbon_alpha_by = NULL,
  ribbon_alpha_range = c(0.15, 0.9),
  ribbon_outline_by = NULL,
  ribbon_outline_colors = NULL,
  ribbon_linetype_by = NULL,
  ribbon_linetypes = NULL,
  ribbon_direction = "none",
  ribbon_direction_colors = c(same = "black", reverse = "grey50"),
  ribbon_direction_linetypes = c(same = "solid", reverse = "dashed"),
  ribbon_direction_alpha = c(same = 1, reverse = 0.45),
  ribbon_ctrl_point,
  region_data = NULL,
  region_fill = "#F59E0B",
  region_color = "#B45309",
  region_alpha = 0.25,
  region_width = 0.08,
  region_offset = 0,
  region_side = "inside",
  ribbon_highlight_rows = integer(0),
  gene_data = NULL,
  geneGap,
  geneWidth,
  geneLabelRadialOffset,
  geneLabelCircumOffset,
  geneLabelCircumLimit,
  geneLabelRotation,
  gene_label_show,
  gene_label_size,
  gene_label_wrap = NULL,
  gene_label_repel_layer = FALSE,
  gene_label_repel_max_overlaps = Inf,
  gene_label_repel_box_padding = 0.25,
  gene_label_repel_point_padding = 0.1,
  gene_label_repel_min_segment_length = 0.5,
  gene_label_repel_force = 1,
  gene_label_repel_seed = 123,
  gene_label_orientation = "arc",
  gene_label_segment = "line",
  gene_label_side = "auto",
  gene_label_segment_linetype = "auto",
  gene_color_scheme,
  gene_colors,
  gene_order,
  seq_label_text = NULL,
  seq_label_radius = NULL,
  seq_label_rotation = NULL,
  seq_label_size = NULL,
  seq_label_orientation = "arc",
  seq_label_hjust = NULL,
  seq_label_vjust = NULL,
  axisGap,
  axisMaj,
  axisMajLen,
  axisMin,
  axisMinLen,
  labelSize,
  labelOffset,
  axisLabelOrientation,
  axis_label_hide_overlaps = FALSE,
  show_axis,
  rotation,
  debug = FALSE
)
}
\arguments{
\item{seqs}{Vector of sequence IDs (order already processed)}

\item{lens}{Named vector of sequence lengths (names = seq_id)}

\item{seq_labels}{Named vector of sequence labels}

\item{seqRadius}{Named vector of sequence radii}

\item{seq_curvature}{Named vector of sequence curvatures}

\item{orientation}{Named vector of sequence orientations (1 or -1)}

\item{seq_gap}{Named vector of sequence gap proportions}

\item{ribbon_data}{Alignment data (already validated)}

\item{ribbonGap}{Named vector of ribbon gaps}

\item{gene_data}{Gene data (already validated)}

\item{gene_label_side}{Character, default "auto". Which side of the arc the
labels sit on: "auto" (strand-based placement), "inside" (toward the chord
center) or "outside" (away from the center, avoiding ribbon overlap).}

\item{gene_label_segment_linetype}{Character or numeric, default "auto".
Leader-line linetype; "auto" uses solid lines except for labels moved to
the other side of their arc, which use dashed lines.}

\item{seq_label_orientation}{Character, default "arc". Sequence label text
orientation: "arc" (rotated along the arc, kept readable) or "horizontal"
(all labels horizontal, extending away from the chord center).}

\item{seq_label_hjust}{Optional named vector or NULL, default NULL. Per-seq
horizontal justification; NULL uses 0.5 (arc mode) or a side-based value
(horizontal mode).}

\item{seq_label_vjust}{Optional named vector or NULL, default NULL. Per-seq
vertical justification; NULL uses 0.5.}

\item{rotation}{Global rotation angle (degrees)}

\item{debug}{Whether to output debug information}
}
\value{
A chord layout list
}
\description{
Pre-computes the coordinates of all geometric elements (sequence arcs, ribbons, gene arrows, axes, etc.)
into Cartesian (x, y) coordinates and stores them in a layout list.
}
\keyword{internal}
