% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ribbon-utils.R
\name{filter_ggchord_ribbons}
\alias{filter_ggchord_ribbons}
\title{Filter alignment ribbons before plotting}
\usage{
filter_ggchord_ribbons(
  ribbon_data,
  seq_ids = NULL,
  min_pident = NULL,
  max_pident = NULL,
  min_length = NULL,
  max_evalue = NULL,
  min_bitscore = NULL,
  min_query_coverage = NULL,
  min_subject_coverage = NULL,
  keep_pairs = NULL,
  drop_self_links = TRUE,
  sort_by = NULL
)
}
\arguments{
\item{ribbon_data}{data.frame with at least \code{qaccver} and
\code{saccver} columns (normally the full ribbon_data format).}

\item{seq_ids}{Optional character vector. Keep only rows where both the
query and the subject are in \code{seq_ids}.}

\item{min_pident, max_pident}{Optional numeric. Lower/upper bounds on
\code{pident}.}

\item{min_length}{Optional numeric. Lower bound on \code{length}.}

\item{max_evalue}{Optional numeric. Upper bound on \code{evalue} (requires
an \code{evalue} column).}

\item{min_bitscore}{Optional numeric. Lower bound on \code{bitscore}
(requires a \code{bitscore} column).}

\item{min_query_coverage}{Optional numeric (0-100). Lower bound on the query
coverage. Uses the \code{qcovs} column when present; otherwise computed
from \code{qlen} and the query interval (requires \code{qlen}).}

\item{min_subject_coverage}{Optional numeric (0-100). Lower bound on the
subject coverage, computed from \code{slen} and the subject interval
(requires an \code{slen} column).}

\item{keep_pairs}{Optional data.frame/list/matrix describing an undirected
set of sequence pairs. A data.frame or matrix with the first two columns
used as query/subject IDs, or a list of length-2 character vectors. A row
is kept when its query/subject pair matches any pair in either direction.}

\item{drop_self_links}{Logical, default \code{TRUE}. Remove rows where
\code{qaccver == saccver}.}

\item{sort_by}{Optional character vector of column names. Prefix a name
with \code{"-"} or \code{"desc:"} for descending order (e.g.
\code{c("pident", "-evalue")}).}
}
\value{
A list with \code{data} (the filtered data frame, with
  \code{source_rows} attribute) and \code{report} (n_input/n_kept/n_removed,
  removed_by_reason, removed_rows and kept_rows).
}
\description{
Keeps the ribbon rows that satisfy all requested criteria and optionally
sorts them. The returned data frame keeps every extra column and the
original column order; the original row numbers are attached as the
\code{source_rows} attribute.
}
\examples{
data(ribbon_data_example)
out <- filter_ggchord_ribbons(
  ribbon_data_example,
  min_pident = 95,
  drop_self_links = TRUE,
  sort_by = c("pident", "-length")
)
out$report
}
