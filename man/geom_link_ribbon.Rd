% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-ribbon.R
\name{geom_link_ribbon}
\alias{geom_link_ribbon}
\title{Add an alignment ribbon layer}
\usage{
geom_link_ribbon(
  mapping = NULL,
  data = NULL,
  ribbon_ctrl_point = NULL,
  ribbon_gap = NULL,
  fill = NULL,
  alpha = 0.42,
  colour = "#59636D",
  linewidth = 0.08,
  linetype = 1,
  position = "identity",
  show.legend = TRUE,
  inherit.aes = FALSE,
  ...
)
}
\arguments{
\item{mapping}{Default NULL (uses pre-computed data)}

\item{data}{Default NULL (retrieved automatically from the layout)}

\item{ribbon_ctrl_point}{Optional vector/list. Bezier control points, default c(0,0)}

\item{ribbon_gap}{Optional numeric/vector controlling spacing between
sequences and ribbon endpoints. The default \code{NULL} uses local
obstacle-aware spacing: endpoints move closer to \code{geom_seq()} where
no \code{geom_gene()} or \code{geom_feature()} polygon overlaps that
genomic interval, and retain enough clearance where one does. Text and
leader lines are ignored. Supplying a number disables the automatic rule
and uses that exact spacing.}

\item{fill}{Optional fixed ribbon fill. `NULL` keeps the default Identity
mapping.}

\item{alpha, colour, linewidth, linetype}{Standard fixed ribbon styles.}

\item{position, show.legend, inherit.aes}{Standard ggplot2 layer arguments.}

\item{...}{Additional arguments passed to \code{geom_polygon()}}
}
\value{
A ggplot2 layer.
}
\description{
Draws alignment ribbons. Identity is mapped to `ribbon_fill` by default;
alternative mappings and legends are controlled with `aes()`, `scale_*()`
and `guides()`.
}
\examples{
library(ggchord)
data(seq_data_example)
data(ribbon_data_example)
p <- ggchord(seq_data_example, ribbon_data_example) +
  geom_seq() + geom_link_ribbon()
p
}
