% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-gene.R
\name{geom_gene_label_repel}
\alias{geom_gene_label_repel}
\title{Add a repelled gene label layer (ggrepel-style)}
\usage{
geom_gene_label_repel(
  mapping = NULL,
  data = NULL,
  gene_label_size = NULL,
  gene_label_rotation = NULL,
  gene_label_radial_offset = NULL,
  gene_label_circum_offset = NULL,
  gene_label_circum_limit = NULL,
  gene_label_wrap = NULL,
  max_overlaps = Inf,
  box_padding = 0.25,
  point_padding = 0.1,
  min_segment_length = 0.05,
  force = 1,
  seed = 123,
  gene_label_orientation = "arc",
  gene_label_segment = "line",
  gene_label_side = "auto",
  gene_label_segment_linetype = "auto",
  show_legend = FALSE,
  ...
)
}
\arguments{
\item{mapping}{Default NULL (uses pre-computed data)}

\item{data}{Default NULL (retrieved automatically from the layout)}

\item{gene_label_size}{Numeric. Label font size, default 2.5}

\item{gene_label_rotation}{Optional numeric/vector/list. Label rotation angle, default 0}

\item{gene_label_radial_offset}{Optional numeric/vector/list. Radial offset of labels, default 0}

\item{gene_label_circum_offset}{Optional numeric/vector/list. Circumferential offset of labels, default 0}

\item{gene_label_circum_limit}{Optional logical/vector/list. Whether to limit circumferential offset, default TRUE}

\item{gene_label_wrap}{Numeric or NULL, default NULL. When set, long gene
annotations are wrapped at this many characters (e.g. 15).}

\item{max_overlaps}{Numeric, default Inf. Hide labels that still overlap
more than this many other labels after repulsion (ggrepel-style
decluttering). Use a finite value to clean up crowded plots.}

\item{box_padding}{Numeric, default 0.25. Extra padding around each label
box (data units).}

\item{point_padding}{Numeric, default 0.1. Extra padding around the anchor
points (data units).}

\item{min_segment_length}{Numeric, default 0.05. Labels that moved less than
this distance (data units) from their anchor do not draw a leader line.
Keep it small so that every label is connected to its gene.}

\item{force}{Numeric, default 1. Strength of the repulsive forces.}

\item{seed}{Numeric, default 123. Random seed for reproducibility.}

\item{gene_label_orientation}{Character, default "arc". One of
\code{"arc"} (text rotated along the sequence arc) or \code{"horizontal"}
(all labels are drawn horizontally).}

\item{gene_label_segment}{Character, default "line". Leader line style: a
straight \code{"line"} from the gene to the label, or an L-shaped
\code{"elbow"} (a short segment outward, then a horizontal segment to the
label). Elbow segment lengths adapt to each label's position and text
width, so labels can be placed freely.}

\item{gene_label_side}{Character, default "auto". Which side of the arc the
labels sit on. \code{"auto"} keeps the strand-based placement
(same as before); \code{"outside"} moves labels that would be inside the
chord (where they can overlap the ribbons) to the outside of their arc;
\code{"inside"} does the opposite. Labels moved to the other side are
connected with a dashed leader line (see \code{gene_label_segment_linetype}).}

\item{gene_label_segment_linetype}{Character or numeric, default "auto".
Leader-line linetype. \code{"auto"} draws solid lines, except for labels
that were moved to the other side of their arc, which are drawn dashed.
Any other valid ggplot2 linetype (e.g. \code{"solid"}, \code{"dashed"},
\code{"dotted"}, or a numeric dash pattern) is used for all leader lines.}

\item{show_legend}{Whether to show the legend, default FALSE}

\item{...}{Additional arguments passed to \code{geom_text()}}
}
\value{
A list of ggplot2 layers (a leader-line layer and a text layer).
}
\description{
Like \code{\link{geom_gene_label}()}, but the labels are placed with a
force-based simulation that pushes them away from the genes and from each
other (similar to \code{ggrepel::geom_text_repel()}). Labels that move far
enough from their anchor are connected to it with a leader line, and labels
that still overlap too many others can be hidden.
}
\examples{
data(seq_data_example)
data(gene_data_example)
p <- ggchord(seq_data_example, gene_data = gene_data_example) +
  geom_seq() + geom_gene() + geom_gene_label_repel()
ggplot2::ggplot_build(p)
}
